import { LocationList, LocationCreate, LocationEdit, LocationShow } from "./Locations";

export default {
    list: LocationList,
    create: LocationCreate,
    edit: LocationEdit,
    show: LocationShow,
}